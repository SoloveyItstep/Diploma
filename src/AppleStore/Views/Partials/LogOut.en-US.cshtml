@model String
@addTagHelper "*, Microsoft.AspNet.Mvc.TagHelpers"

<div class="loggedin-page">
    <div class="col-xs-12 loggedin-title text-center" style="margin-top: 15px;">
        <span>Info</span>
    </div>
    <div class="loggedin-info-text">
        <div class="col-xs-12">
            <span>You are authorized</span><br />
            <span>as @Model</span>
        </div>
        <span class="error-text text-danger"></span>
    </div>
    <div class="loggedin-byttons">
        <button type="button" class="popup-register-button logout">Logout</button>
        <br /><br />
        <button type="button" class="popup-register-button cancel-logout">Cancel</button>
    </div>
</div>
<script type="text/javascript">
    $(".authorization").css("left", "57%");
    $(".logout").click(function () {
        $.ajax({
            type: "POST",
            url: "/Auth/Logout",
            success: function (response) {
                if (response == false) {
                    $(".error-text").html("There was an error<br/>Please try again later");
                }
                else if (response == true) {
                    console.log("LogOut");
                    $(".dark_background").trigger("click");
                    $(".lk").attr("title", "");
                    $("#lk-img").attr("src", "/images/HomeLayout/lk.png");
                    $("#lk-img").attr("onmouseout", "this.src = '/images/HomeLayout/lk.png'");
                }
            }
        });
    });
    $(".cancel-logout").click(function () {
        $(".dark_background").trigger("click");
    });
</script>

